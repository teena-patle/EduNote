# EduNote - Minimal Working Project
## How to run locally
1. Install Node.js and MongoDB.
2. Clone or extract the project.
3. Run `npm install`
4. Start MongoDB (e.g., `mongod`)
5. Run `npm run dev` (requires nodemon) or `npm start`
6. Open http://localhost:3000
